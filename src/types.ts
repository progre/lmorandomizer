export interface InitialParameters {
  seed: string;
  installDirectory: string;
  easyMode: boolean;
}

export interface Settings {
  seed?: string;
  installDirectory?: string;
  easyMode?: boolean;
}
